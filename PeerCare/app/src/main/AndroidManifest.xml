<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.vogella.android.retrofitgithub">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".dashboard"></activity>
        <activity android:name=".ActivityHome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".authentication.signup.SignUpThirdScreen" />
        <activity android:name=".authentication.signup.SignUpSecondScreen" />
        <activity android:name=".menu.Menu" />
        <activity android:name=".authentication.signup.SignUp" />
        <activity android:name=".authentication.signin.Login" />
        <activity
            android:name=".authentication.MainAuthMenu"
            android:label="@string/title_activity_main_auth_menu"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>